/* CRM1 Follow-ups: preload active follow-up customer data so callback rows never wait for a second query. */
(()=>{'use strict';const $=id=>document.getElementById(id);let observer=null,timer=null,cleaning=false,ready=false,cache=[],cacheAt=0,loadPromise=null;const TTL=15000;const active=()=>$('followups')?.classList.contains('active');const hide=r=>r.style.setProperty('visibility','hidden','important');const show=r=>r.style.setProperty('visibility','visible','important');const isActive=s=>['pending','open','scheduled','followup'].includes(String(s||'').trim().toLowerCase());const norm=s=>String(s||'').toLowerCase().replace(/[,.]/g,'').replace(/\s+/g,' ').trim();const istKey=v=>{const d=new Date(v);return Number.isNaN(d)?'':norm(new Intl.DateTimeFormat('en-IN',{timeZone:'Asia/Kolkata',day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',hour12:true}).format(d))};function col(t,n){return[...t.querySelectorAll('thead th')].findIndex(h=>new RegExp('^'+n+'$','i').test(h.textContent.trim()))}function statusIdx(t){return col(t,'Status')}async function load(force=false){const db=window.sb;if(!db)return cache;if(!force&&cache.length&&Date.now()-cacheAt<TTL)return cache;if(loadPromise)return loadPromise;loadPromise=db.from('followups').select('id,order_id,customer_id,followup_at,status,note,notes,customers(customer_name,mobile)').in('status',['pending','open','scheduled','followup']).order('followup_at',{ascending:true}).limit(1000).then(({data,error})=>{if(!error){cache=data||[];cacheAt=Date.now()}else console.warn('Follow-up preload failed',error);return cache}).catch(e=>{console.warn(e);return cache}).finally(()=>loadPromise=null);return loadPromise}function setCard(root,label,n){for(const e of root.querySelectorAll('*'))if((e.textContent||'').trim().toLowerCase()===label){const b=e.parentElement?.querySelector('b');if(b)b.textContent=n;return}}function counts(root,data){const s=new Date();s.setHours(0,0,0,0);const e=new Date(s);e.setDate(e.getDate()+1);let o=0,t=0,u=0;(data||[]).filter(x=>isActive(x.status)).forEach(x=>{const d=new Date(x.followup_at);if(d<s)o++;else if(d<e)t++;else u++});setCard(root,'overdue',o);setCard(root,'today',t);setCard(root,'upcoming',u)}function enrich(table,data){const ci=col(table,'Customer');let wi=col(table,'When'),mi=col(table,'Mobile');if(ci<0||wi<0)return;if(mi<0){const heads=[...table.querySelectorAll('thead th')],h=document.createElement('th');h.textContent='Mobile';heads[ci].after(h);for(const tr of table.querySelectorAll('tbody tr')){const td=tr.querySelectorAll('td')[ci];if(td){const m=document.createElement('td');m.textContent='-';td.after(m)}}mi=ci+1;if(wi>ci)wi++}const pool=(data||[]).map(x=>({...x,_used:false,_key:istKey(x.followup_at)})),ni=col(table,'Note');for(const tr of table.querySelectorAll('tbody tr')){const c=tr.querySelectorAll('td');if(c.length<=wi)continue;const key=norm(c[wi]?.textContent),note=ni>=0?norm(c[ni]?.textContent):'';let x=pool.find(v=>!v._used&&v._key===key);if(!x&&note)x=pool.find(v=>!v._used&&norm(v.note||v.notes)===note);if(!x)continue;x._used=true;const cc=tr.querySelectorAll('td')[ci],mc=tr.querySelectorAll('td')[mi];if(cc)cc.textContent=x.customers?.customer_name||'-';if(mc)mc.textContent=x.customers?.mobile||'-'}}async function clean(){if(cleaning||!active())return;const root=$('followupsContent'),table=root?.querySelector('table');if(!root||!table)return;cleaning=true;try{const data=await load();const si=statusIdx(table);if(si>=0)[...table.querySelectorAll('tbody tr')].forEach(tr=>{const c=tr.querySelectorAll('td');if(c.length&&String(c[si]?.textContent||'').trim().toLowerCase()==='completed')tr.remove()});enrich(table,data);counts(root,data);ready=true;show(root);load(true).then(d=>{if(active())counts(root,d)});}finally{cleaning=false}}function schedule(ms=0){clearTimeout(timer);timer=setTimeout(clean,ms)}function begin(){const root=$('followupsContent');if(!root)return;ready=false;hide(root);load();schedule(0);[50,150,350,700].forEach(ms=>setTimeout(()=>{if(active()&&!ready)schedule(0)},ms))}document.addEventListener('click',e=>{const b=e.target.closest('button,a');if(b&&/follow-ups/i.test(b.textContent||''))begin()},true);function observe(){const root=$('followupsContent');if(!root||observer)return;observer=new MutationObserver(()=>{if(active()&&!ready)schedule(0)});observer.observe(root,{childList:true,subtree:true,characterData:true})}const boot=()=>{load();observe();if(active())begin()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.crm1CleanFollowups=begin;})();