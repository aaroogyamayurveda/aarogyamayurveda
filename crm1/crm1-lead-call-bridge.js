/* CRM1 Lead Call Bridge: binds Lead modal to Telephony bar without changing core Lead Manager. */
(function(){'use strict';const $=id=>document.getElementById(id);function digits(v){return String(v||'').replace(/\D/g,'').slice(-10)}function boot(){document.addEventListener('click',e=>{const b=e.target?.closest('#oCall');if(!b)return;setTimeout(()=>{const modal=$('modalContent');if(!modal)return;const leadTitle=$('modalTitle')?.textContent||'';const phoneInput=document.querySelector('#crm1DialNumber');if(!phoneInput)return;const rows=[...document.querySelectorAll('#crmLeadBody tr')];const row=rows.find(r=>r.textContent.includes(leadTitle.replace(/^Lead:\s*/,'')));const mobile=row?digits(row.children?.[1]?.textContent):'';if(!mobile)return;const openBtn=[...document.querySelectorAll('.crmLeadOpen')].find(x=>x.closest('tr')===row);const leadId=openBtn?.dataset?.id||null;phoneInput.value=mobile;if(leadId&&window.crm1SetCallContext)window.crm1SetCallContext({lead_id:leadId});window.dispatchEvent(new CustomEvent('crm1LeadCallReady',{detail:{lead_id:leadId,mobile}}));},0)},true)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();})();
