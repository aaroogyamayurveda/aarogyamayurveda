/* CRM1 single remarks source: lightweight integration without global DOM observers. */
(function(){'use strict';const $=id=>document.getElementById(id);let bound=false;function apply(){const page=$('createOrderPage');if(!page)return false;const orderRemarks=page.querySelector('textarea[name="remarks"]');if(orderRemarks){const f=orderRemarks.closest('.field');const label=f?.querySelector('label');if(label)label.textContent='Remarks'}const disp=$('crm1InlineDisposition');if(disp){const field=$('cdRemarks')?.closest('.field');if(field)field.style.display='none';const save=$('cdSave');if(save&&!bound){bound=true;save.addEventListener('click',()=>{const hidden=$('cdRemarks');const current=$('createOrderPage')?.querySelector('textarea[name="remarks"]');if(hidden)hidden.value=(current?.value||'').trim()},true)}}return !!(orderRemarks&&disp)}function boot(){let tries=0;const timer=setInterval(()=>{tries++;if(apply()||tries>=40)clearInterval(timer)},250);window.addEventListener('crm1WorkspaceCall',apply);window.addEventListener('crm1WorkspaceReset',apply)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();})();
