/* CRM1 Call Disposition v2: schema-safe. Follow-up is stored only on crm_leads. */
(function(){'use strict';const URL='https://ielebadardbzmoxantsc.supabase.co',KEY='sb_publishable_0pekrOT6vhYZYQ48wHr7Ag_NPcpobGj';const $=id=>document.getElementById(id);let db,me,lastInteraction=null,ctx={lead_id:null,customer_id:null};async function boot(){for(let i=0;i<20&&!window.supabase?.createClient;i++)await new Promise(r=>setTimeout(r,250));if(!window.supabase?.createClient)return;db=window.supabase.createClient(URL,KEY);const {data:{user}}=await db.auth.getUser();if(!user)return;me=user;document.addEventListener('click',e=>{if(e.target?.id==='crm1EndCall')setTimeout(openLatest,400);if(e.target?.id==='crm1LogCall')setTimeout(openManual,200)})}function setContext(x={}){ctx={lead_id:x.lead_id||null,customer_id:x.customer_id||null};window.crm1CallContext={...ctx}}window.crm1SetCallContext=setContext;async function resolveContext(n){if(ctx.lead_id||!n)return ctx;const {data}=await db.from('crm_leads').select('id,customer_id').or(`mobile.eq.${n},alternate_mobile.eq.${n}`).order('updated_at',{ascending:false}).limit(1).maybeSingle();if(data)setContext(data);return ctx}async function openLatest(){const {data}=await db.from('crm_interactions').select('*').eq('agent_id',me.id).eq('interaction_type','call').order('started_at',{ascending:false}).limit(1).maybeSingle();if(!data)return;if(data.lead_id||data.customer_id)setContext(data);else await resolveContext(data.provider_payload?.dial_number||null);show(data)}async function openManual(){const n=($('crm1DialNumber')?.value||'').replace(/\D/g,'').slice(-10);await resolveContext(n);show({id:null,details:'Manual call: '+n})}function show(x){lastInteraction=x;const m=$('modal');if(!m)return;$('modalTitle').textContent='Call Disposition';$('modalContent').innerHTML='<div class="crm-adv-form"><div class="field wide"><label>Call Outcome</label><select id="cdOutcome"><option value="connected">Connected</option><option value="not_connected">Not Connected</option><option value="callback">Callback Required</option><option value="interested">Interested</option><option value="not_interested">Not Interested</option><option value="wrong_number">Wrong Number</option><option value="converted">Converted</option></select></div><div class="field wide"><label>Remarks</label><textarea id="cdRemarks" rows="3" placeholder="Call summary / customer response"></textarea></div><div class="field wide" id="cdFollowWrap" style="display:none"><label>Next Follow-up</label><input id="cdFollow" type="datetime-local"></div></div><div class="actions"><button class="btn" id="cdSave">Save Disposition</button></div>';m.style.display='grid';$('cdOutcome').onchange=()=>{$('cdFollowWrap').style.display=$('cdOutcome').value==='callback'?'':'none'};$('cdSave').onclick=save}async function save(){const outcome=$('cdOutcome').value,remarks=$('cdRemarks').value.trim(),follow=$('cdFollow').value?new Date($('cdFollow').value).toISOString():null;if(outcome==='callback'&&!follow){alert('Select next follow-up date and time');return}const ids={lead_id:ctx.lead_id||lastInteraction?.lead_id||null,customer_id:ctx.customer_id||lastInteraction?.customer_id||null};const patch={...ids,status:outcome==='converted'?'completed':outcome,disposition:outcome,details:remarks||lastInteraction?.details||null,ended_at:new Date().toISOString()};let error;if(lastInteraction?.id)({error}=await db.from('crm_interactions').update(patch).eq('id',lastInteraction.id));else({error}=await db.from('crm_interactions').insert({...patch,interaction_type:'call',direction:'outbound',provider:'manual',agent_id:me.id,created_by:me.id,started_at:new Date().toISOString()}));if(error){alert(error.message);return}if(ids.lead_id){const leadPatch={last_contact_at:new Date().toISOString()};if(outcome==='callback'){leadPatch.next_followup_at=follow;leadPatch.lead_status='follow_up'}else if(outcome==='converted'){leadPatch.next_followup_at=null;leadPatch.lead_status='converted'}else if(['not_interested','wrong_number'].includes(outcome)){leadPatch.next_followup_at=null;leadPatch.lead_status='lost'}else if(['interested','connected'].includes(outcome)){leadPatch.lead_status='follow_up';if(follow)leadPatch.next_followup_at=follow}if(remarks)leadPatch.notes=remarks;const r=await db.from('crm_leads').update(leadPatch).eq('id',ids.lead_id);if(r.error){alert(r.error.message);return}}window.dispatchEvent(new CustomEvent('crm1DataChanged',{detail:{type:'disposition',lead_id:ids.lead_id,customer_id:ids.customer_id,follow_up_at:follow}}));ctx={lead_id:null,customer_id:null};window.crm1CallContext={...ctx};$('modal').style.display='none';alert('Disposition saved'+(follow?' and follow-up scheduled':''))}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();})();
